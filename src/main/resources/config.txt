# Configuration file to be copied to target/config when executing run.sh,
# to check if the app is loading that external file

greeting.message = Hello Quarkus from external configuration file at target/config/

# configure your datasource
#quarkus.datasource.url  = jdbc:h2:./classes/META-INF/resources/h2_db_dev
dev.quarkus.datasource.url  = jdbc:h2:./classes/META-INF/resources/h2_db_dev
prod.quarkus.datasource.url = jdbc:h2:./classes/META-INF/resources/h2_db_prod
quarkus.datasource.driver = org.h2.Driver
#quarkus.hibernate-orm.database.generation  = update

quarkus.http.port  = 8080